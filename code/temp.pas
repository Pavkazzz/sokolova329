{Splitted:=strList[i].Split([':='], None); //че-то парсим
    if Splitted[0]<>'' then //че-то сравиваем
        DrawShape('rect',splitted[0]); //че-то рисуем
    
{ blockSizeConn:=blockSizeConn.Create(
      leftPos-multiplier-round(bmp.Width/2),
      topPos-multiplier-Round(bmp.Height/2),
      leftPos+round(bmp.Width/2)+multiplier,
      topPos+Round(bmp.Height/2)+multiplier
      );

      Canvas.StretchDraw(blockSizeConn,bmp);
      }
      
